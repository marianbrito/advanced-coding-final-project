SECRET_KEY = 'marian123'  # Secret key used by Flask for session management and CSRF protection.
MYSQL_HOST = "localhost"  # Hostname of the MySQL server (usually 'localhost' for local development).
MYSQL_USER = "root"  # Username for connecting to the MySQL database.
MYSQL_PASSWORD = ''  # Password for the MySQL user (leave empty if no password is set).
MYSQL_DB = 'flask_users'  # Name of the database to be used in the MySQL server.

